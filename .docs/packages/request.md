# Request - 网络请求

由于我们需要兼容多端，所以统一封装了一个底层的网络请求库这里我们采用的是 axios 在封装的过程中，我们只是将底层进行了适配，并未涉及到任何业务逻辑，如果需要了解请求的具体判断，可以参考 `apps/public/apis`中的各端实现

## 使用方式

具体可参考 `apps/public/apis`中的各端实现

## 支持的参数

由于我们是继承的 axios，所以支持的参数都和 axios 一致，这里就不一一列举了
