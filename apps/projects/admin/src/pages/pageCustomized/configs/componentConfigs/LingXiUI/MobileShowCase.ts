import { ComponentSchemaType, PROPS_SETTING_TYPES } from '@apps/design-core'

const MobileShowCase: ComponentSchemaType = {
  propsConfig: {
    componentType: {
      label: '编辑',
      type: PROPS_SETTING_TYPES.mobileShowCase,
    },
  },
}

export default MobileShowCase
