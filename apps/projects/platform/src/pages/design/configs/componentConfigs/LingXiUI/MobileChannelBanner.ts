import { ComponentSchemaType, PROPS_TYPES } from '@apps/design-core'

const MobileChannelBanner: ComponentSchemaType = {
  propsConfig: {
    componentType: {
      label: '编辑',
      type: 'mobileChannelBanner' as PROPS_TYPES,
    },
  },
}

export default MobileChannelBanner
