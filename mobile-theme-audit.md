# Mobile 小程序主题 UI 审计与当日提交记录

> 审计日期：2026-08-11  
> 审计分支：`links-web-source-code`  
> 审计范围：`apps/projects/mobile`  
> 主题基准：`theme-change.md`

## 1. 文档目的

本文档用于：

1. 汇总 Mobile 小程序当前已经完成或部分完成的主题更新范围。
2. 梳理仍残留旧青绿色主题、需要继续更新的页面。
3. 给出各页面的进入操作步骤，方便在微信开发者工具中逐项验收。
4. 记录 2026-08-11 在当前分支已经提交的修改。

## 2. 主题更新约束与基准

后续主题更新继续遵守以下约束：

- 只修改颜色相关样式，不调整页面布局、组件层级和交互逻辑。
- 不修改宽高、间距、字号、行高、圆角、定位和图片尺寸。
- 页面背景使用 `#FCF7F1`。
- 通用容器背景使用 `#FFFCF8`。
- 卡片背景使用 `#FDF9F5`。
- 卡片外描边使用 `#EAD9CD`。
- 卡片阴影使用 `0 4px 12px rgba(125, 70, 35, 0.12)`。
- 品牌主色使用 `#C45124`，按下态使用 `#A83F1B`。
- 主要文字使用 `#5A2A12`，次要文字使用 `#7A5743`，弱提示文字使用 `#8F7564`。
- Tab 选中文字、下划线、主要按钮、重点链接等品牌元素统一使用陶橙主题色。
- Button、Checkbox、Radio、Tab、筛选选中态等公共交互组件均需同步主题色。
- 红、绿、黄、蓝业务状态色按语义保留，不机械替换为品牌橙。

## 3. 静态审计概览

本次基于路由配置、页面源码、公共组件和硬编码颜色进行静态审计，结果如下：

| 审计项                     | 数量 | 说明                                               |
| -------------------------- | ---: | -------------------------------------------------- |
| 已注册 Mobile 路由         |  215 | 包含主包和各分包页面                               |
| 明确残留旧主题的路由       |   69 | 页面源码仍直接使用旧青绿色或旧浅绿色主题           |
| 需要人工复核的冷灰页面候选 |   98 | 未必存在旧绿色，但背景、文字、边框仍可能是冷灰体系 |
| 旧主题色源码命中           |  187 | 当前静态检查结果，需结合页面语义处理               |
| `#00A98F`                  |  137 | 旧品牌主色                                         |
| `#EBF9F6`                  |   37 | 旧品牌浅底色                                       |
| `#E4F7EF`                  |   10 | 旧品牌浅底色                                       |
| `#00B37A`                  |    3 | 旧绿色变体                                         |

> 说明：静态命中不等于页面一定可见，也不代表可以全文替换。角色权限、订单状态、活动状态和业务数据会影响页面入口及元素显示，最终仍需在微信开发者工具中逐页检查。

## 4. 已完成或已部分完成的主题范围

| 模块 | 当前情况 | 后续注意事项 |
| --- | --- | --- |
| 首页 | 已更新暖色背景、导航、营销卡片、商品卡片、店铺卡片等 | 继续检查动态活动组件和低频入口 |
| 商品列表 | 商品卡片、筛选排序 Tab 已更新 | 检查不同商城、搜索结果和特殊商品列表是否共用同一组件 |
| 普通商品详情 | 卡片、底部加入购物车/立即购买按钮已更新 | 拼团、专供、套餐等特殊详情页仍需单独处理 |
| 分类页面 | Tab 下划线已更新 | 继续检查分类子页面和分类导航扩展页 |
| 登录页 | 部分主题与勾选状态已更新 | 注册、找回密码、多账户等流程仍需复核 |
| 我的 | 页面背景、卡片、部分图标和文字已更新 | 常用工具进入的二级、三级页面仍有较多残留 |
| 店铺会员 | 数字颜色等局部主题已更新 | 会员权益和不同等级状态仍需验证 |
| 我的订单 | 搜索、Tab、卡片、确认收货按钮已更新 | 其他订单状态、支付流程、售后流程需继续处理 |
| 订单详情 | 顶部状态、物流入口、卡片颜色已更新 | 不同状态订单需要逐项验收 |
| 物流详情 | 已新增 Mock 物流详情页面，并使用 Taro Map | 后续接接口时保留现有页面与动画结构 |
| 余额账户 | 账户详情、交易记录、提现页面已更新 | 充值、绑卡、认证和其他记录页仍需处理 |
| 常见问题 | 顶部区域已更新 | 帮助详情页面仍需复核 |
| 无网络页面 | 重试按钮主题已更新 | 检查禁用态、按下态和不同平台表现 |

## 5. 明确残留旧主题的页面

下表共覆盖 69 个明确残留旧主题的路由。进入步骤以普通用户可见入口为主；需要特殊角色、订单状态或活动数据的页面已在“重点检查”中注明。

### 5.1 商品、订单、询价与报价

| 模块 | 页面路由 | 进入页面的操作步骤 | 重点检查 |
| --- | --- | --- | --- |
| 特殊商品详情 | `commodityMerge/stocksSourcing/index`<br>`commodityMerge/stocksSourcing/detailGroup`<br>`commodityMerge/stocksSourcing/shareGroupDetail`<br>`commodityMerge/soleSourcing/detail` | 首页/分类/搜索 → 商品列表 → 点击对应商品；拼团分享详情可从拼团入口或分享链接进入 | 页面背景、信息卡片、规格选中态、价格、底部按钮、Tab；摄像头和播放器保持停用状态 |
| 订单确认与状态 | `order/ConfirmOrder`<br>`order/integral`<br>`order/statusLayout`<br>`order/editRfqSubmitSuccess` | 底部购物车 → 勾选商品 → 结算；积分商城 → 商品 → 兑换；提交订单或询报价后进入结果页 | 地址卡片、支付方式、优惠区域、提交按钮、成功/失败状态色 |
| 询价单 | `order/inquiry`<br>`order/inquiry/inquiryDetail` | 我的 → 功能服务 → 询价单 → 点击询价单 | 顶部 Tab、筛选选中态、状态文字、详情卡片、底部操作按钮 |
| 报价单 | `order/inquiryQuotation`<br>`order/inquiryQuotation/inquiryQuotationDetail` | 我的 → 功能服务 → 报价单 → 点击报价单 | 顶部 Tab、筛选选中态、报价状态、价格、确认/提交按钮 |

### 5.2 寻源需求

| 模块 | 页面路由 | 进入页面的操作步骤 | 重点检查 |
| --- | --- | --- | --- |
| 采购方寻源 | `askPurchase/buyer/list`<br>`askPurchase/detail`<br>`askPurchase/buyer/add`<br>`askPurchase/buyer/edit`<br>`askPurchase/buyer/feedback`<br>`askPurchase/offerDetail`<br>`askPurchase/supplierList` | 我的 → 功能服务 → 寻源需求 → 列表；点击新增、编辑、详情、反馈、报价详情或供应商列表 | 页面背景、Tab、筛选、表单选中态、上传区、卡片、底部新增/提交按钮 |
| 供应方寻源 | `askPurchase/merchants/list`<br>`askPurchase/merchants/quoteList`<br>`askPurchase/quoteDetail`<br>`askPurchase/add`<br>`askPurchase/skuList`<br>`askPurchase/merchants/feedback`<br>`askPurchase/list` | 切换到供应商账号 → 我的 → 功能服务 → 寻源管理/报价管理 → 点击记录、报价、SKU 或反馈 | 需要供应商角色；重点检查状态 Tab、报价表单、商品选择、底部按钮和空状态 |

### 5.3 售后、社区团购、团长与分销

| 模块 | 页面路由 | 进入页面的操作步骤 | 重点检查 |
| --- | --- | --- | --- |
| 售后主流程 | `afterService/afterTodo/applyAs`<br>`afterService/afterRecords/refundRecords`<br>`afterService/afterRecords/voucherList` | 我的 → 我的订单/退换售后 → 选择订单 → 申请售后；或订单详情 → 申请售后 | 售后类型、原因选中态、凭证上传、状态标签、列表卡片、提交按钮 |
| 社区团购 | `communityGroupBuy/list`<br>`communityGroupBuy/changeSelfPickupAddress`<br>`communityGroupBuy/activityDetail`<br>`communityGroupBuy/productDetail` | 首页 → 社区团购/活动入口 → 活动列表 → 选择自提点 → 活动详情/商品详情 | 活动状态、倒计时、地址选中态、商品卡片、规格、底部购买按钮 |
| 团长中心 | `teamLeader/mine`<br>`teamLeader/apply`<br>`teamLeader/applicationForm`<br>`teamLeader/applySuccess`<br>`teamLeader/detail`<br>`teamLeader/groupBuyDetail`<br>`teamLeader/enrolledActivityList`<br>`teamLeader/groupPurchaseOrders`<br>`teamLeader/receiptList`<br>`teamLeader/receiptDetail`<br>`teamLeader/agentPickup`<br>`teamLeader/selfPickup` | 我的 → 功能服务 → 团长中心；未申请用户进入申请流程，已通过用户从团长中心菜单进入活动、订单、核销和自提页面 | 需要分别使用未申请、审核中、已通过团长账号；检查顶部主题、统计卡片、状态色、Tab 和操作按钮 |
| 分销中心 | `distribution/mine`<br>`distribution/apply`<br>`distribution/goods` | 我的 → 功能服务 → 分销中心；非分销员进入申请，分销员进入商品列表 | 申请状态、收益卡片、商品卡片、佣金颜色、分享/推广按钮 |

### 5.4 个人资料、安全、商城、资讯与其他流程

| 模块 | 页面路由 | 进入页面的操作步骤 | 重点检查 |
| --- | --- | --- | --- |
| 个人资料与设置 | `basicSetting/userInfo`<br>`basicSetting/accountSettings`<br>`basicSetting/addressList`<br>`basicSetting/addressAdd`<br>`basicSetting/invoiceAdd` | 我的 → 点击头像/编辑资料；我的 → 常用工具 → 收货地址/发票抬头 → 新增或编辑 | 表单背景、输入框、默认地址开关、单选/勾选、保存按钮 |
| 安全、验证与帮助 | `basicSetting/accountSafe`<br>`basicSetting/enterpBindphone`<br>`basicSetting/footprint`<br>`basicSetting/HelpCenter/details` | 我的 → 常用工具 → 账号安全/足迹/常见问题；企业或 e 账户认证流程 → 绑定手机 | 验证步骤、验证码按钮、勾选状态、列表卡片、帮助详情标题与链接 |
| 店铺与商城切换 | `shop/home`<br>`shop/ingralCommodityList`<br>`shop/shopAbout`<br>`extra/mall/own/select` | 首页 → 推荐店铺 → 进店；店铺首页 → 积分商品/店铺信息；点击商城名称或切换入口 → 选择商城 | 店铺头部、Tab、商品卡片、商城选中态、底部导航 |
| 资讯中心 | `companyNews/newsHome`<br>`companyNews/newsInformation`<br>`companyNews/newsMy` | 首页 → 资讯/推荐内容 → 更多 → 资讯详情；资讯中心 → 我的资讯 | 分类 Tab、资讯卡片、收藏/关注状态、空状态 |
| 评价与收藏 | `extra/evaluatingManage`<br>`members/collection` | 我的 → 常用工具 → 我的评价/我的收藏；评价也可从订单卡片进入 | 评分颜色、标签选中态、商品卡片、提交评价按钮、空状态 |
| 特殊用户流程 | `user/multAccInfoList`<br>`user/store`<br>`user/paySuccess` | 登录后存在多个主体时进入多账户列表；注册/入驻流程进入店铺信息；支付完成进入支付成功页 | 账号选中态、表单按钮、支付成功状态色、返回/继续操作按钮 |

## 6. 需要人工复核的冷灰页面候选

以下页面未必直接包含旧品牌绿色，但页面背景、容器、文字、边框、禁用态或局部组件仍可能保留冷灰体系。建议在完成 69 个明确旧主题页面后继续逐页复核。

| 模块 | 候选页面 | 进入页面的操作步骤 | 复核重点 |
| --- | --- | --- | --- |
| 订单与支付 | `order/Purchase`、`order/payList`、`order/payOrder`、`order/offlineTransfer`、`order/SubmitSuccess`、`order/selfMention`、`order/editRfqOrder`、`order/editRfqOtherInfo`、`order/editRfqOrderProduct`、`order/oftenBuy` | 购物车/商品详情 → 结算 → 支付；我的订单 → 对应订单操作；我的 → 常购清单 | 暖白背景、支付方式卡片、输入框、选中态、提交按钮、成功状态 |
| 售后剩余流程 | 退款商品编辑、退款寄回、确认收货、凭证查看、退款完成、退款详情、售后物流；换货记录/详情/寄回/收货/完成；维修记录/详情/完成；选择物流公司等页面 | 我的 → 退换售后 → 选择不同售后类型和状态 → 查看详情或继续操作 | 不同售后状态需要准备数据；检查状态色、物流卡片、上传凭证、底部操作按钮 |
| e 账户与资金 | 充值、绑定银行卡、个人/企业认证、资金记录及详情、未绑定状态、账户首页等页面 | 我的 → 余额账户/e 账户 → 充值、绑卡、认证、记录 | 账户首页、交易记录和提现已部分更新；继续检查卡片、金额、认证步骤和按钮 |
| 账号安全子页面 | 身份验证、手机验证、支付密码、登录密码、实名认证、注销账户等页面 | 我的 → 常用工具 → 账号安全 → 选择具体安全项 | 步骤条、验证码、输入框、警示信息、勾选框、提交按钮 |
| 消息、会员与发票 | 消息中心、会员相关二级页、发票列表及详情 | 我的 → 消息/会员中心/发票抬头 → 点击列表项 | Tab、未读状态、会员等级、发票卡片和操作按钮 |
| 分销二级页面 | `distribution/addShop`、`distribution/invitation`、`distribution/list`、`distribution/detail`、`distribution/reward` | 我的 → 分销中心 → 店铺、邀请、分销列表、详情、奖励 | 收益数字、二维码/邀请区域、列表卡片、状态色 |
| 团长补充页面 | `teamLeader/groupBuyList`、`teamLeader/scanVerify` | 我的 → 团长中心 → 团购列表/扫码核销 | 扫码结果、核销状态、列表卡片、操作按钮 |
| 店铺二级页面 | `shop/pointExchange`、积分详情、`shop/shopSearch`、`shop/findShop`、`shop/popularShop` | 店铺首页 → 积分兑换/搜索；首页 → 找店/热门店铺 | 搜索框、筛选 Tab、积分颜色、店铺卡片、兑换按钮 |
| 资讯二级页面 | `companyNews/newsClassify`、`companyNews/newsRecommend`、`companyNews/newsSearchList`、`companyNews/newsHistoryList` | 资讯中心 → 分类/推荐/搜索/浏览记录 | Tab、搜索、资讯卡片、时间文字、空状态 |
| 用户流程 | 找回密码、修改密码、注册、完善资料、企业类型、角色选择、支付、扫码登录等页面 | 登录页 → 找回密码/快速注册；注册流程 → 完善资料/选择角色；扫码入口 → 扫码登录 | 表单、验证码、协议勾选、角色选中态、下一步和提交按钮 |
| 活动及其他 | `activity/index`、`categoryNavigation/index`、`integralMall`、`showcase`、`switchCity`、分类公共页、评价页面等 | 首页活动入口、分类导航、积分商城、橱窗、城市切换或订单评价入口进入 | 活动状态、分类选中态、城市选中态、商品卡片、积分和按钮 |

## 7. 公共组件与主题入口残留

公共组件残留会同时影响多个页面，应优先于逐页修改处理。

| 文件或组件 | 当前问题 | 建议处理 |
| --- | --- | --- |
| `apps/projects/mobile/src/constants/colors.ts` | 动态主题主色仍可能使用 `DESIGN_GREEN` | 将运行时品牌色映射到陶橙主题，保留业务成功色独立语义 |
| `src/components/FilterModal/index.scss` | 选中背景或边框仍可能使用 `#EBF9F6` | 统一为品牌淡底 `#FCE9DE` 和品牌边框色 |
| `src/styles/utils/modal.scss` | 弹窗主按钮仍可能使用 `#00A98F` | 统一弹窗主按钮、按下态、禁用态 |
| `src/components/ChannelActionSheet/index.tsx` | 选中勾选图标仍可能为绿色 | 改为品牌主色，业务成功图标除外 |
| `ModeCountryCode`、`Modemobile` | 手机号或国家区号选中态仍可能为绿色 | 同步品牌主色和浅色选中背景 |
| `Progress` | 默认进度条 stroke 仍可能为绿色 | 业务进度使用品牌色；成功进度保留成功色 |
| 团长、分销、社区团购共享组件 | 多页面共用旧绿色样式 | 先修共享组件，再检查业务页面局部硬编码 |

## 8. 建议处理顺序

### 第一优先级：公共残留和动态主题

1. 处理 `constants/colors.ts`、弹窗、筛选、勾选、Progress 等公共主题入口。
2. 检查公共 Button、Checkbox、Radio、Tab、筛选组件的默认色、选中色、按下色和禁用色。
3. 修改后优先回归首页、商品列表、商品详情、我的、订单列表，确认已完成页面未被公共样式反向影响。

### 第二优先级：核心交易链路

1. 商品特殊详情页。
2. 购物车、订单确认、支付、订单状态页。
3. 询价单、报价单及详情。
4. 每种订单状态至少准备一条测试数据，检查按钮和状态文字。

### 第三优先级：账户与售后链路

1. e 账户充值、绑卡和认证。
2. 账号安全、地址、发票。
3. 退款、换货、维修、物流和凭证页面。

### 第四优先级：角色业务链路

1. 采购方与供应方寻源。
2. 团长中心。
3. 分销中心。
4. 社区团购。

这些页面需要准备不同角色和审核状态账号，不能只检查默认账号。

### 第五优先级：低频与内容页面

1. 资讯、店铺二级页、活动和积分商城。
2. 注册、找回密码、多账户、入驻等用户流程。
3. 空状态、无网络、加载、禁用、成功、失败等边界状态。

## 9. 微信开发者工具验收建议

每完成一组页面，按以下清单验收：

- 页面布局、宽高、间距、字号、圆角和定位与修改前一致。
- 页面背景、卡片背景、边框和阴影符合主题基准。
- 当前 Tab、下划线、主按钮、勾选框和筛选选中态为陶橙主题。
- 卡片统一为 `#FDF9F5`、`#EAD9CD` 外描边和暖色阴影。
- 商品价格和重点金额使用品牌主色，但错误、成功、警告状态保留正确业务语义。
- 主按钮、次按钮、禁用按钮、按下态和加载态均清晰可辨。
- 使用真实角色和业务数据验证权限页面、订单状态页和售后状态页。
- 编译通过仅代表静态构建成功，不能替代微信开发者工具中的实际页面检查。

## 10. 2026-08-11 已提交修改记录

今天在 `links-web-source-code` 分支共完成 13 次提交。下表按提交时间从早到晚排列，提交哈希和提交标题均取自 Git 历史。

| 时间 | 提交 | 提交标题 | 修改摘要 | 变更统计 |
| --- | --- | --- | --- | --: |
| 10:25:14 | `77e361d3` | 注释 ezplayer 插件配置 | 注释小程序 ezplayer 插件注册及商品详情分包插件配置，解决微信开发者工具插件注册报错 | 2 个文件，+10/-9 |
| 10:27:30 | `8b277328` | 更新本地开发接口地址 | 更新 `ytt.config.ts` 和 `env.dev.js` 的本地开发接口配置 | 2 个文件，+3/-3 |
| 12:38:02 | `5bfd9a1f` | `[mobile] 同步首页卡片主题样式` | 建立暖色主题变量，更新首页导航、营销、商品、品牌、资讯、橱窗和店铺卡片，并新增 `theme-change.md` | 31 个文件，+392/-77 |
| 15:22:12 | `0008a736` | `[mobile] 暂停详情页摄像头功能` | 暂停普通详情页和拼团详情页的摄像头请求及播放器区域 | 3 个文件，+13/-447 |
| 15:24:50 | `71003a48` | `[mobile] 更新微信开发者工具配置` | 更新微信开发者工具公共和私有项目配置 | 2 个文件，+8/-6 |
| 16:09:47 | `0fa0ff9f` | `【mobile】同步商品卡片与微信开发者工具配置` | 统一商品列表卡片主题，同时同步微信开发者工具配置 | 3 个文件，+31/-138 |
| 16:39:27 | `1b39d755` | `【mobile】统一详情页按钮主题` | 更新商品详情卡片和加入购物车、立即购买等按钮主题，并处理相关商品组件 | 5 个文件，+83/-6 |
| 17:19:50 | `c4623af5` | `【mobile】统一商品筛选与Tab主题色` | 统一分类、商品列表、商城全部商品及订单相关 Tab、筛选排序的主题色 | 6 个文件，+23/-5 |
| 18:12:06 | `ee2441aa` | `【mobile】更新会员我的与订单主题` | 更新店铺会员数字、我的页面背景与卡片、订单列表卡片和确认收货等主题 | 14 个文件，+92/-69 |
| 18:23:44 | `f9391419` | `【mobile】更新账户与寻源页面主题` | 更新筛选弹窗、采购寻源列表、e 账户首页和绑定手机勾选主题 | 5 个文件，+19/-16 |
| 18:41:53 | `3575585f` | `【mobile】统一账户与提现主题` | 更新账户详情、交易记录、交易详情、提现区域及确认提现按钮主题 | 9 个文件，+70/-54 |
| 19:02:59 | `963909c7` | `【mobile】统一帮助与订单顶部主题` | 更新常见问题顶部 UI、订单详情顶部状态和收货信息区域主题 | 7 个文件，+22/-17 |
| 20:56:24 | `7b494323` | `【mobile】新增订单物流详情并优化相关主题` | 新增 Mock 物流详情页和 Taro Map；增加订单列表“查看物流”、订单详情物流信息入口；调整无网络按钮和页面返回体验 | 13 个文件，+469/-49 |

### 10.1 当日修改结果汇总

- 已停用 ezplayer 插件注册，以及普通商品详情和拼团详情的摄像头/播放器功能。
- 已同步开发环境接口配置和微信开发者工具配置。
- 已确定 Mobile 小程序暖白、陶橙、深棕、米杏主题基准。
- 已更新首页、商品卡片、商品列表、普通商品详情、分类和筛选 Tab。
- 已更新我的、店铺会员、我的订单、订单详情、账户详情、交易记录、提现、常见问题和无网络页面的部分主题。
- 已新增独立的订单物流详情页面，使用 Mock 数据和 Taro Map，不修改原有 `order/logistics` 页面。
- 已在待收货订单列表增加“查看物流”，并在订单详情增加最新物流信息入口。

## 11. 后续维护要求

- 后续新增页面或修改页面时，Button、Tab、Checkbox、Radio、筛选选中态和卡片样式默认同步当前主题。
- 卡片样式保持统一，不再使用旧纯白卡片、冷灰边框或青绿色阴影。
- 新增提交信息使用中文括号格式，例如：`【mobile】统一售后页面主题`。
- 用户后续提出“提交”时，默认含 Git commit 和 push。
- 每批主题更新完成后同步更新本文档中的完成状态、待处理页面和提交记录。
