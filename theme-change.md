# 移动端主题颜色优化方案

## 1. 方案目标

参考效果图，将当前以青绿色 `#00A98F` 为主的视觉主题，调整为更贴合农产品、认养和原产地业务的配色：

- 暖白作为页面与卡片基底，降低纯白、冷灰带来的生硬感。
- 陶橙作为品牌主色，用于按钮、选中态、价格和重点操作。
- 深棕作为主要文字色，形成自然、温暖、可信赖的品牌气质。
- 米杏用于边框、分割线和浅色强调背景。
- 保留红、绿、黄、蓝各自的业务状态含义，不把所有状态色都替换成品牌橙。

初步实施范围限定为移动端项目 `apps/projects/mobile`。`mobile-sale`、`mobile-srm`、PC 管理端及后端不在本次范围内。

## 2. 强制约束

本次后续实施必须遵守以下约束：

1. 不修改页面结构、组件层级、排列方式和交互逻辑。
2. 不修改任何宽度、高度、最小/最大宽高。
3. 不修改 `margin`、`padding`、`gap`、定位坐标等间距。
4. 不修改字号、行高、字重、字体和文字截断规则。
5. 不修改现有组件的圆角、尺寸和间距；首页卡片为本次明确的视觉细节调整范围，使用不改变盒模型尺寸的 1px 外描边，并采用固定的暖色阴影参数。
6. 不修改图片、图标尺寸，也不替换业务图片和 Banner 素材。
7. 只允许调整以下颜色内容：
   - `color`
   - `background`、`background-color` 中的颜色值
   - `border-color`、`outline-color`
   - `fill`、`stroke`
   - `box-shadow` 中的颜色值；首页卡片按本方案补充固定的 `0 4px 12px` 暖色阴影
   - 渐变中的颜色值，渐变角度和色标位置保持原样
   - TS/TSX 中传给图标、导航栏和组件的颜色属性

## 3. 参考图颜色结论

参考图的页面背景实测接近 `#FCF7F1`，卡片底色接近 `#FDF9F5`，搜索按钮主体接近 `#CD5F2E`。为保证白色小字号文字在主色按钮上的可读性，实施色板将主色略微加深为 `#C45124`。

整体风格定义为：**暖白、陶橙、深棕、米杏、少量金色点缀**。

## 4. 建议主题色板

### 4.1 品牌色

| 语义名称 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 品牌主色 `brand-primary` | `#C45124` | 主按钮、选中态、价格、重要链接、活动入口 |
| 品牌浅色 `brand-light` | `#E59A73` | 次级强调、浅色图标、进度条浅色部分 |
| 品牌深色 `brand-dark` | `#A83F1B` | 按下态、激活态、深色强调文字 |
| 品牌淡底 `brand-soft` | `#FCE9DE` | 标签底色、选中项浅底、提示区域 |
| 品牌极淡底 `brand-softest` | `#FFF2E9` | 浅色悬浮区、弱强调背景 |
| 反色文字 `text-inverse` | `#FFFFFF` | 品牌色按钮和深色背景上的文字 |

### 4.2 背景与容器色

| 语义名称 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 页面背景 `bg-page` | `#FCF7F1` | 页面整体背景、底部导航背景 |
| 主容器背景 `bg-surface` | `#FFFCF8` | 弹窗、搜索框、表单容器等通用容器 |
| 卡片背景 `card-bg` | `#FDF9F5` | 首页导航卡片、信息卡片及同类内容卡片 |
| 次级容器背景 `bg-subtle` | `#FFF5EC` | 分组区域、筛选区、浅色模块 |
| 纯白背景 `bg-white` | `#FFFFFF` | 需要最高对比度的内容区域，不做全局替换 |
| 遮罩背景 `bg-mask` | 保留现有黑色透明遮罩 | 弹窗遮罩、图片文字遮罩，避免改成棕色后影响层级识别 |

### 4.3 文字与图标色

| 语义名称 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 主要文字 `text-primary` | `#5A2A12` | 页面标题、商品名、正文、主要导航 |
| 次要文字 `text-secondary` | `#7A5743` | 辅助信息、次级标题、未选中导航 |
| 弱提示文字 `text-muted` | `#8F7564` | 时间、说明、输入框占位文字 |
| 禁用文字 `text-disabled` | `#BFAFA3` | 禁用状态、不可操作提示 |
| 主要图标 `icon-primary` | `#5A2A12` | 普通导航和操作图标 |
| 弱图标 `icon-muted` | `#A98F7C` | 箭头、关闭、辅助操作图标 |

### 4.4 边框、分割线和阴影色

| 语义名称 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 强调边框 `border-brand` | `#D97A4D` | 搜索框、选中卡片、重点输入框 |
| 常规边框 `border-default` | `#E8CBB8` | 卡片、输入框、次按钮 |
| 卡片外描边 `card-border` | `#EAD9CD` | 首页卡片 1px 外描边，不改变卡片盒模型尺寸 |
| 弱边框 `border-subtle` | `#F0DED1` | 分割线、列表边界、浅色容器边界 |
| 阴影颜色 `shadow-warm` | `rgba(125, 70, 35, 0.12)` | 首页卡片暖色阴影，参数固定为 `0 4px 12px` |

首页卡片细节（本次修订）：

- 卡片背景统一使用 `#FDF9F5`，与页面背景 `#FCF7F1` 保持轻微层次差异。
- 使用 `1px solid` 等效的外描边色 `#EAD9CD`；实现采用 `outline`，避免改变原有宽高和内容布局。
- 使用 `0 4px 12px rgba(125, 70, 35, 0.12)` 柔和暖色阴影，不修改卡片圆角、宽高、边距和内边距。

### 4.5 辅助与状态色

| 语义名称 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 金色点缀 `accent-gold` | `#C78A3A` | 会员、积分、等级、认证标签 |
| 成功色 `status-success` | `#5F7D3B` | 成功、已完成、在线状态 |
| 警告色 `status-warning` | `#B97828` | 待处理、库存提醒、风险提示 |
| 错误色 `status-error` | `#BC3F34` | 错误、失败、删除、异常状态 |
| 信息色 `status-info` | `#466F82` | 普通信息、系统提示 |

说明：当前组件库中 `$color-success` 跟随品牌主色。切换为陶橙后必须将成功色独立为绿色，避免“成功”状态显示成橙色。

## 5. 当前颜色到新主题的替换建议

以下为语义明确时的替换关系，不进行无差别全文替换：

| 当前常见颜色 | 建议替换 | 说明 |
| --- | --- | --- |
| `#00A98F` | `#C45124` | 当前品牌主色改为陶橙主色 |
| `#00957E` | `#A83F1B` | 当前品牌深色改为陶橙按下态 |
| `#5AD6B7` | `#E59A73` | 当前品牌浅色改为陶橙浅色 |
| `#EBF9F6`、`#E4F7EF` | `#FCE9DE` | 当前绿色浅底改为品牌淡底 |
| `#252D37`、`#303133` | `#5A2A12` | 主要文字由冷黑灰改为深棕 |
| `#5C626A`、`#606266` | `#7A5743` | 次要文字由冷灰改为中棕 |
| `#91959B`、`#909399` | `#8F7564` | 弱提示文字改为暖棕灰 |
| `#C8CACD`、`#C0C4CC` | `#BFAFA3` | 禁用文字和弱图标改为暖灰棕 |
| `#F4F5F7`、`#F5F6F7` | `#FCF7F1` | 冷灰页面底改为暖白页面底 |
| `#F7F8FA`、`#FAFBFC` | `#FFFCF8` | 浅灰容器改为暖白容器 |
| `#EDEEEF`、`#EEF0F3`、`#EBECF0` | `#F0DED1` | 冷灰边框和分割线改为米杏色 |

以下颜色禁止机械替换：

- `#FFFFFF`、`#FFF`：根据页面背景、卡片和反色文字分别判断。
- `#000000`、`#000`：正文可改深棕，遮罩和透明黑仍保留。
- 红、黄、绿、蓝状态色：先判断错误、警告、成功、信息语义，再映射到对应状态色。
- 图表、数据可视化和业务自定义色：保留数据区分能力，不统一改为品牌橙。
- 图片、Logo、Banner 和商品图中的颜色：本次不处理。

## 6. 计划修改位置

确认后按以下顺序实施：

### 第一阶段：移动端主题入口

- `apps/projects/mobile/src/styles/variables.scss`
  - 统一 SCSS 品牌色、文字色、背景色、边框色和状态色。
  - 只调整或新增颜色变量，不改任何尺寸变量。
- `apps/projects/mobile/src/constants/theme.ts`
  - 将 TS 侧主色从 `#00A98F` 调整为新主色。
  - `themeLayout` 中所有间距值保持不变。
- `apps/projects/mobile/src/constants/colors.ts`
  - 调整动态主题色板，使运行时颜色与 SCSS 主题一致。

原则上不直接修改共享文件 `apps/public/mobile-ui/packages/styles/variables/default.scss`，优先在 `apps/projects/mobile` 内覆盖，避免影响 `mobile-sale` 和 `mobile-srm`。

### 第二阶段：全局组件和公共组件

仅修改组件中的颜色值，重点覆盖：

- 按钮、搜索框、输入框、标签、复选框、单选框和开关。
- 顶部导航、Tabs、底部导航、选中态和未选中态。
- 卡片、列表、弹窗、分割线、空状态和加载状态。
- 商品价格、会员/积分标签、主要操作图标。
- TSX 中直接写入的 `color="#00A98F"` 等颜色属性。

### 第三阶段：页面硬编码颜色

当前移动端源码中存在较多硬编码颜色，其中 `#00A98F` 约 175 处，另有大量冷灰背景和文字色。后续逐页面按语义替换，不使用简单的全局批量替换，避免破坏：

- 错误、警告、成功等状态表达。
- 遮罩层透明度和图片上的文字可读性。
- 图表、数据标签和业务分类色。
- 第三方组件自身必要的颜色约定。

## 7. 重点页面效果映射

| 页面元素 | 调整方向 |
| --- | --- |
| 页面整体背景 | 冷灰或纯白调整为 `#FCF7F1` |
| 顶部标题和普通导航 | 调整为 `#5A2A12` |
| 搜索框背景 | 调整为 `#FFFCF8` |
| 搜索框边框 | 调整为 `#D97A4D` |
| 搜索按钮 | 调整为 `#C45124`，文字保持白色 |
| 当前 Tab 和下划线 | 调整为 `#C45124` |
| 非当前 Tab | 调整为 `#5A2A12` 或 `#7A5743` |
| 卡片背景 | 调整为 `#FDF9F5` |
| 卡片边框 | 使用 `#EAD9CD` 的 1px 外描边，不改变卡片盒模型尺寸 |
| 卡片阴影 | 使用 `0 4px 12px rgba(125, 70, 35, 0.12)` 暖色柔和阴影 |
| 卡片内部分割线 | 调整为 `#F0DED1` |
| 商品名称 | 调整为 `#5A2A12` |
| 商品价格 | 调整为 `#C45124` |
| 次级说明 | 调整为 `#7A5743` 或 `#8F7564` |
| 底部导航选中项 | 调整为 `#C45124` |
| 底部导航未选中项 | 调整为 `#5A2A12` |

首页其他卡片同步范围：

- 营销卡片、商品卡片、横向商品卡片和店铺卡片统一使用卡片底色、外描边和暖色阴影。
- 推荐品牌卡片、资讯卡片、橱窗卡片和自营品牌卡片同步使用同一套卡片细节。
- 卡片内部的状态标签、优惠标签和业务渐变保留原有语义，仅调整容器层级颜色；不改变图片、尺寸和排版。

## 8. 验收标准

### 8.1 代码差异检查

- Git 差异中不得出现布局结构和组件层级变更。
- 不得修改宽高、间距、字号、行高、圆角和定位值；首页卡片新增的外描边和阴影不计入盒模型尺寸。
- 除首页卡片按本方案新增的固定阴影外，对已有复合属性只允许改变颜色部分。
- 不修改接口、路由、状态管理和业务逻辑。

### 8.2 构建检查

- 执行微信小程序构建，确认无 SCSS、TS、TSX 编译错误。
- 构建成功只代表静态编译通过，不代替微信开发者工具中的页面检查。

### 8.3 微信开发者工具检查

- 首页、搜索、分类、商品列表、商品详情、购物车、个人中心。
- 弹窗、表单、空状态、加载状态、禁用状态。
- 成功、警告、错误、信息提示的颜色语义正确。
- 主要文字和按钮文字清晰可读。
- 与修改前对比，所有页面布局、尺寸和交互保持一致。

## 9. 本次修订确认项

本次首页卡片修订已按以下值执行，后续视觉验收和微调以此为基准：

1. 调整范围仅为 `apps/projects/mobile`，其他前端子项目不动。
2. 采用 `#C45124` 作为新品牌主色。
3. 采用 `#FCF7F1` 作为主要页面暖白背景。
4. 采用 `#5A2A12` 作为主要文字深棕色。
5. 图片、Logo、Banner 和商品图不做调色或替换。
6. 优先在移动端项目内覆盖主题，不直接改共享 `mobile-ui` 默认主题。
7. 首页卡片背景、外描边和阴影按本次修订值执行：`#FDF9F5`、`#EAD9CD`、`0 4px 12px rgba(125, 70, 35, 0.12)`。

后续仍按“主题入口 → 公共组件 → 页面硬编码颜色 → 构建与微信开发者工具检查”的顺序验收。
