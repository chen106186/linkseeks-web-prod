# 瓴犀前端项目启动手册

本文档适用于当前前端项目 `linkseeks-web-prod`，用于本地开发、接口代码生成、前端项目启动、构建和常见故障排查。

## 1. 项目位置

前端项目根目录：

```text
E:\1024-code\00-company\linkseeks\linkseeks-web-prod
```

以下命令默认都在这个目录执行。

## 2. 项目组成

前端是 pnpm workspace，主要项目如下：

| 项目        | 目录                        | 用途                       |
| ----------- | --------------------------- | -------------------------- |
| admin       | `apps/projects/admin`       | 平台后台                   |
| platform    | `apps/projects/platform`    | 能力中心、商家端后台       |
| mall        | `apps/projects/mall`        | PC 商城，包含 SSR 服务     |
| im          | `apps/projects/im`          | 智能客服                   |
| mobile      | `apps/projects/mobile`      | 商城微信小程序和 H5        |
| mobile-sale | `apps/projects/mobile-sale` | 业务员小程序和 H5          |
| mobile-srm  | `apps/projects/mobile-srm`  | SRM 小程序和 H5            |
| public      | `apps/public`               | 公共配置、接口、组件和工具 |
| packages    | `packages`                  | 公共基础库                 |

公共环境配置位于 [apps/public/config](./apps/public/config)，接口生成配置位于 [apps/public/apis](./apps/public/apis)。

## 3. 开发环境要求

推荐版本：

```text
Node.js 20.18.3
pnpm 10.4.1
```

检查版本：

```powershell
node --version
pnpm --version
```

项目虽然声明 Node.js `>=14`，但当前依赖和文档推荐 Node.js 20。Node.js 24 如果出现 Vite、Taro、依赖编译或运行异常，优先切换到 Node.js 20.18.3。

使用 nvm 时：

```powershell
nvm use 20.18.3
```

如果本机未安装 pnpm：

```powershell
npm install --global pnpm@10.4.1
```

## 4. 首次安装依赖

```powershell
cd E:\1024-code\00-company\linkseeks\linkseeks-web-prod
pnpm install
```

项目根目录存在 `pnpm-lock.yaml`。需要严格按照锁文件安装时：

```powershell
pnpm install --frozen-lockfile
```

如果安装阶段仅因 Husky 报错，且当前不需要 Git 提交前检查：

```powershell
$env:HUSKY = '0'
pnpm install
```

## 5. 环境配置

环境选择入口是 [apps/public/config/index.js](./apps/public/config/index.js)：

| `ENV_TYPE` | 配置文件      | 含义       |
| ---------- | ------------- | ---------- |
| `dev`      | `env.dev.js`  | 开发环境   |
| `test`     | `env.site.js` | 测试环境   |
| `uat`      | `env.uat.js`  | 预发布环境 |
| `prod`     | `env.prod.js` | 生产环境   |

当前默认环境为 `dev`。PowerShell 临时指定环境：

```powershell
$env:ENV_TYPE = 'dev'
```

环境变量只对当前 PowerShell 窗口有效。修改环境后必须重新启动开发进程。

### 5.1 主要环境变量

| 变量                        | 用途                                     |
| --------------------------- | ---------------------------------------- |
| `BACK_GATEWAY`              | 浏览器运行时的后端网关地址               |
| `YAPI_REQUEST_BACK_GATEWAY` | 获取 YApi 接口 token 的网关地址          |
| `SOCKET_URL`                | WebSocket 地址                           |
| `SITE_URL`                  | PC 商城站点地址                          |
| `MEMBER_URL`                | 能力中心地址                             |
| `IM_URL`                    | 客服跳转地址                             |
| `REQUEST_HEADER`            | 请求协议，通常是 `http://` 或 `https://` |

当前 `dev` 默认配置位于 [env.dev.js](./apps/public/config/env.dev.js)，重点地址为：

```text
BACK_GATEWAY=http://192.168.110.137:8100
YAPI_REQUEST_BACK_GATEWAY=http://192.168.110.137:8100
SOCKET_URL=ws://lingxi-gateway-test-2023-v3.shushangyun.com:12880
```

如果团队确认了其他可访问网关，可以不修改文件，直接临时覆盖：

```powershell
$env:ENV_TYPE = 'dev'
$env:BACK_GATEWAY = 'http://可访问的网关地址:端口'
$env:SOCKET_URL = 'ws://可访问的WebSocket地址:端口'
pnpm start:admin
```

`BACK_GATEWAY` 影响页面运行时请求；`YAPI_REQUEST_BACK_GATEWAY` 主要影响接口 token 同步。YApi 页面地址不能直接当作业务网关地址。

## 6. YApi 接口代码生成

### 6.1 YApi 地址

当前 YApi 地址：

```text
http://192.168.110.15:3000/group/11
```

接口生成器的 YApi 服务地址配置在 [ytt.config.ts](./apps/public/apis/ytt.config.ts)。

### 6.2 推荐流程：已有接口配置，直接从 YApi 生成

当 YApi 可访问，并且项目已有本地接口配置时：

```powershell
pnpm --filter @apps/apis api-local
```

该命令直接使用 YApi 生成接口代码，不执行后端 token 列表同步，适合 YApi 可访问但旧网关不确定的场景。

生成结果位于：

```text
apps/public/apis/src/services
```

### 6.3 完整流程：先同步 token，再生成接口

```powershell
pnpm api
```

根目录的 `pnpm api` 会调用 `@apps/apis` 的 `api` 脚本。完整流程会先请求：

```text
<YAPI_REQUEST_BACK_GATEWAY>/manage/deploy/yapi/token/list
```

然后再从 YApi 拉取接口。

如果当前默认网关不可访问，但团队已提供可访问的 token 同步网关：

```powershell
$env:YAPI_REQUEST_BACK_GATEWAY = 'http://可访问的网关地址:端口'
pnpm api
```

接口生成日志可能包含 YApi token 或内部地址，不要完整复制到群聊、工单或公开日志中。

### 6.4 什么时候需要重新生成

以下情况需要重新生成接口：

- 新增或修改了 YApi 接口；
- `apps/public/apis/src/services` 缺少对应服务；
- 接口类型与 YApi 定义不一致；
- 接口生成时报导入错误，确认是生成目录缺失。

如果生成目录已经存在且接口没有变化，可以跳过生成，直接启动前端。

## 7. 前端启动命令

### 7.1 PC 管理后台

```powershell
pnpm start:admin
```

访问：

```text
http://localhost:4399
```

### 7.2 能力中心

```powershell
pnpm start:platform
```

访问：

```text
http://localhost:4396
```

### 7.3 PC 商城

```powershell
pnpm start:mall
```

默认访问：

```text
http://localhost:6002
```

商城使用 Node SSR 服务启动。临时更换端口：

```powershell
$env:PORT = '6003'
pnpm start:mall
```

### 7.4 智能客服

```powershell
pnpm start:im
```

访问：

```text
http://localhost:4400
```

### 7.5 商城微信小程序和 H5

微信小程序：

```powershell
pnpm start:weapp
```

H5：

```powershell
pnpm start:h5
```

小程序命令启动 Taro watch 构建，构建完成后使用微信开发者工具打开终端输出的构建目录；H5 实际访问地址以终端输出为准。

### 7.6 业务员端

```powershell
pnpm start:sale-weapp
pnpm start:sale-h5
```

### 7.7 SRM 端

```powershell
pnpm start:srm-weapp
pnpm start:srm-h5
```

## 8. 各环境启动

当前根目录提供部分环境快捷命令：

```powershell
# 测试环境
pnpm test:admin
pnpm test:platform
pnpm test:mall
pnpm test:im
pnpm test:weapp
pnpm test:h5

# UAT 环境
pnpm uat:admin
pnpm uat:platform
pnpm uat:mall
pnpm uat:weapp
pnpm uat:h5

# 生产环境配置启动，仅用于验证配置或构建，不建议用于日常开发
pnpm prod:admin
pnpm prod:platform
pnpm prod:mall
pnpm prod:weapp
pnpm prod:h5
```

也可以统一使用 PowerShell：

```powershell
$env:ENV_TYPE = 'test'
pnpm start:admin
```

注意：`test` 对应 `env.site.js`，不是 `env.test.js`。

## 9. 启动端口和请求链路

| 本地项目  |         默认端口 | 运行时后端配置              |
| --------- | ---------------: | --------------------------- |
| admin     |             4399 | `BACK_GATEWAY`              |
| platform  |             4396 | `BACK_GATEWAY`              |
| mall SSR  |             6002 | `BACK_GATEWAY`              |
| im        |             4400 | `BACK_GATEWAY`、`/receiver` |
| mobile H5 | 以 Taro 输出为准 | `BACK_GATEWAY`              |

PC 项目基本请求链路：

```text
浏览器
  -> localhost:4399 或 localhost:4396
  -> Vite /api 代理
  -> BACK_GATEWAY
  -> 后端 Gateway
  -> Nacos 注册的微服务
```

YApi 不在页面运行时请求链路中，只用于生成 TypeScript 接口代码。

PC 项目的 `/receiver` 代理当前配置为 `http://10.0.1.212:8002`，客服或消息相关功能异常时需要单独确认该地址。

## 10. 前后端依赖关系

### 10.1 只启动前端页面

满足以下条件即可启动页面：

- Node.js 和 pnpm 版本正确；
- 依赖已经安装；
- 接口生成目录存在，或当前页面不依赖生成接口；
- `BACK_GATEWAY` 指向可访问的后端网关。

只启动前端不要求本地启动 Nacos，也不要求本地启动所有 Java 微服务。

### 10.2 前端调用远程后端

常见调试方式是：

- 前端在本地启动；
- `/api` 由 Vite 代理到远程 Gateway；
- 登录、权限和业务数据由远程后端提供；
- YApi 只在接口代码需要更新时访问。

页面能打开但登录、列表或保存接口失败时，优先检查 `BACK_GATEWAY`，不要先检查 YApi。

### 10.3 启动 Java 后端

后端工程位于同级目录：

```text
E:\1024-code\00-company\linkseeks\micro-services-prod
```

后端 `local` profile 默认依赖 Nacos：

```text
Nacos: 192.168.110.15:8849
YApi: 192.168.110.15:3000
namespace: server
group: v1.0.0
```

数据库、Redis、RabbitMQ、Gateway 等配置由 Nacos 下发。启动 Java 服务前需要确认 Nacos 配置、数据库、缓存和消息队列都可用。

如果只调试前端，不要因为前端启动失败就修改 Nacos 配置。

## 11. 构建命令

推荐针对具体项目执行构建：

```powershell
pnpm build:admin
pnpm build:platform
pnpm build:mall
pnpm build:im
pnpm build:h5
pnpm build:weapp
pnpm build:sale-weapp
```

SRM 和业务员端也可以直接按 workspace 执行：

```powershell
pnpm --filter @projects/mobile-srm build:weapp
pnpm --filter @projects/mobile-srm build:h5
pnpm --filter @projects/mobile-sale build:weapp
pnpm --filter @projects/mobile-sale build:h5
```

移动端构建时也可以明确指定环境：

```powershell
pnpm test-build:weapp
pnpm uat-build:weapp
pnpm prod-build:weapp
```

## 12. 当前根脚本注意事项

当前根目录以下脚本是递归自调用，不要直接执行：

```text
pnpm dev
pnpm build
pnpm lint
pnpm test
```

例如根目录的 `dev` 内容是 `pnpm run dev`，会再次调用自身。请使用明确的项目命令：

```powershell
pnpm start:admin
pnpm start:platform
pnpm build:admin
pnpm build:platform
```

根脚本 `start:mall-platform` 当前没有对应的 `apps/projects/mall-platform` 项目目录，不要使用该命令。

## 13. 常见问题排查

### 13.1 页面能打开，但接口全部失败

检查当前环境变量：

```powershell
Get-ChildItem Env:ENV_TYPE,Env:BACK_GATEWAY,Env:YAPI_REQUEST_BACK_GATEWAY,Env:SOCKET_URL
```

确认：

1. `BACK_GATEWAY` 是业务 Gateway 地址，不是 YApi 地址；
2. 目标网关端口可以访问；
3. 浏览器开发者工具中的请求是否通过本地 `/api`；
4. 是否出现 `502`、`401` 或 `403`。

| 现象                  | 优先判断                             |
| --------------------- | ------------------------------------ |
| `502`、`ECONNREFUSED` | 网关地址或网关端口不可达             |
| `401`                 | 登录态、token 或会话失效             |
| `403`                 | 用户权限、菜单权限或后端权限校验     |
| 接口 `404`            | 网关路由、接口版本或生成代码不一致   |
| WebSocket 失败        | `SOCKET_URL` 或 WebSocket 路径不可达 |

### 13.2 `pnpm api` 失败，但 YApi 页面可以打开

`pnpm api` 不只访问 YApi，还会先访问：

```text
<YAPI_REQUEST_BACK_GATEWAY>/manage/deploy/yapi/token/list
```

因此 YApi 页面可访问，不代表 token 同步网关可访问。可以使用：

```powershell
# 使用当前已有接口配置，直接访问 YApi
pnpm --filter @apps/apis api-local

# 或者给 token 同步配置一个确认可访问的网关
$env:YAPI_REQUEST_BACK_GATEWAY = 'http://可访问的网关地址:端口'
pnpm api
```

### 13.3 接口导入路径找不到

```powershell
Test-Path .\apps\public\apis\src\services
Get-ChildItem .\apps\public\apis\src\services | Select-Object -First 10
```

目录缺失或服务文件不完整时：

```powershell
pnpm --filter @apps/apis api-local
```

### 13.4 Vite 启动端口被占用

```powershell
netstat -ano | Select-String -Pattern ':4399|:4396|:4400|:6002'
```

确认占用进程后，优先关闭对应旧开发进程；不要在不确认 PID 的情况下结束系统进程。

商城可以临时换端口：

```powershell
$env:PORT = '6003'
pnpm start:mall
```

### 13.5 Node.js 24 启动异常

切换到 Node.js 20.18.3 后重新打开 PowerShell：

```powershell
node --version
pnpm --version
pnpm start:admin
```

不要先删除 `node_modules`。只有确认依赖损坏或切换版本后仍异常时，才重新安装依赖。

### 13.6 小程序启动后微信开发者工具报错

检查：

1. 是否使用了对应的 `start:weapp` 命令；
2. 是否使用微信开发者工具打开正确的 Taro 构建目录；
3. AppID、项目配置和白名单是否正确；
4. 小程序构建时使用的 `BACK_GATEWAY` 是否可访问。

### 13.7 Nacos 页面打不开

Nacos 只影响 Java 后端服务启动，不是本地 Vite 页面启动的必要条件。启动后端前确认：

```text
http://192.168.110.15:8849/nacos/
```

并确认 `server` 命名空间、`v1.0.0` 分组及服务所需配置存在。不要把 YApi 的 `3000` 端口改成 Nacos 地址，也不要把 Nacos 地址当作前端 `BACK_GATEWAY`。

## 14. 推荐的日常启动顺序

### 14.1 管理后台日常开发

```powershell
cd E:\1024-code\00-company\linkseeks\linkseeks-web-prod

# 首次安装或 lockfile 有变化时执行
pnpm install

# 只有接口代码需要更新时执行
pnpm --filter @apps/apis api-local

# 启动管理后台
$env:ENV_TYPE = 'dev'
pnpm start:admin
```

浏览器访问：

```text
http://localhost:4399
```

### 14.2 能力中心日常开发

```powershell
cd E:\1024-code\00-company\linkseeks\linkseeks-web-prod
$env:ENV_TYPE = 'dev'
pnpm start:platform
```

浏览器访问：

```text
http://localhost:4396
```

### 14.3 前端连接指定网关

```powershell
cd E:\1024-code\00-company\linkseeks\linkseeks-web-prod
$env:ENV_TYPE = 'dev'
$env:BACK_GATEWAY = 'http://确认可访问的网关:端口'
$env:YAPI_REQUEST_BACK_GATEWAY = 'http://确认可访问的网关:端口'
pnpm start:admin
```

如果只运行已经生成好的前端代码，可以只设置 `BACK_GATEWAY`；只有重新执行 `pnpm api` 时才需要重点设置 `YAPI_REQUEST_BACK_GATEWAY`。

## 15. 变更配置前的检查清单

- [ ] 目标地址是业务 Gateway，而不是 YApi 或 Nacos 控制台；
- [ ] 目标端口从当前电脑可以访问；
- [ ] 是否需要同时修改 WebSocket 地址；
- [ ] 是否只需要当前 PowerShell 临时生效；
- [ ] 是否会影响其他项目或其他开发人员；
- [ ] 是否需要重新生成接口代码；
- [ ] 是否包含 token、密码或内部服务地址，避免提交或外发敏感信息。

## 16. 相关源码和文档

- [前端根 package.json](./package.json)
- [环境选择入口](./apps/public/config/index.js)
- [开发环境配置](./apps/public/config/env.dev.js)
- [YApi 生成配置](./apps/public/apis/ytt.config.ts)
- [接口生成脚本](./apps/public/apis/package.json)
- [管理后台 Vite 配置](./apps/projects/admin/vite.config.ts)
- [能力中心 Vite 配置](./apps/projects/platform/vite.config.ts)
- [项目原始文档目录](./.docs)
- [项目原始新手文档](./.docs/新手文档.md)
- [后端项目说明](../micro-services-prod/README.md)
